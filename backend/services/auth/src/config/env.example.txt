# Copy this file to .env and update with your values

# Server Configuration
PORT=3002

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=linkvesta
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# Email Configuration (for email verification)
# Option 1: Gmail (recommended for development)
# 1. Enable 2-factor authentication on your Gmail account
# 2. Generate an app password: https://myaccount.google.com/apppasswords
# 3. Use your Gmail address and the app password below
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here

# Option 2: Custom SMTP (for production)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_email@example.com
# SMTP_PASS=your_password
# SMTP_FROM=noreply@linkvesta.com

# Frontend URL (for email verification links)
FRONTEND_URL=http://localhost:3000

# CORS Configuration (comma-separated list of allowed origins)
# For production, replace with your actual domain(s)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Database Connection Pool Configuration (for scalability)
DB_POOL_MAX=50              # Maximum database connections (default: 50)
DB_POOL_MIN=5               # Minimum database connections (default: 5)
DB_POOL_IDLE_TIMEOUT=30000  # Idle timeout in milliseconds (default: 30000)
DB_POOL_CONNECTION_TIMEOUT=10000  # Connection timeout in milliseconds (default: 10000)

# Rate Limiting Configuration (customize for your needs)
RATE_LIMIT_GENERAL_MAX=200           # General API requests per window (default: 200)
RATE_LIMIT_GENERAL_WINDOW_MS=900000  # Window in milliseconds = 15 minutes (default: 900000)
RATE_LIMIT_AUTH_MAX=10               # Login attempts per window (default: 10)
RATE_LIMIT_AUTH_WINDOW_MS=900000     # Window in milliseconds = 15 minutes (default: 900000)
RATE_LIMIT_REGISTRATION_MAX=5        # Registrations per hour (default: 5)
RATE_LIMIT_PASSWORD_RESET_MAX=5      # Password resets per hour (default: 5)

# Proxy/Load Balancer Configuration
TRUST_PROXY=false  # Set to true if behind proxy/load balancer (default: false in dev, true in production)